void setup(){
  print("starting sketch...");
  print();
  
  
  fill(100, 130, 10);
  ellipse(width/2, 0, 40, 40);
    
  save("/images/output.jpeg"); //save image to images folder
  print("...sketch done");
  exit(); //stop running
}